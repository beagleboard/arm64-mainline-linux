before_script:
  - export PATH=/usr/lib/ccache:$PATH
  - echo "Acquire::http::Proxy \"http://192.168.1.10:3142\";" > /etc/apt/apt.conf.d/00aptproxy
  - apt-get update
  - apt-get dist-upgrade -yq
  - cd /opt/linux-src/ ; git pull ; cd -
  - pip3 install --break-system-packages --root-user-action=ignore git+https://git.gfnd.rcn-ee.org/mirror/dt-schema.git@main
  - dt-validate --version

build-debian-12-amd64:
  image: robertcnelson/beagle-devscripts-kernel-debian-12-amd64:latest
  # https://openbeagle.org/beagleboard/ci-docker-images
  cache:
    key: "$CI_PROJECT_NAME-debian-12-amd64"
    paths:
      - ccache.tar.lz4
  tags:
    - docker-amd64
  stage: build
  script:
    - git config --global user.email "$GITLAB_USER_EMAIL"
    - git config --global user.name "$GITLAB_USER_NAME"
    - . version.sh && echo $BRANCH
    - mkdir -p /root/.cache/ccache/ || true
    - tar --use-compress-program=lz4 -xf ccache.tar.lz4 -C / || true
    - ccache -M 1G
    - ccache -z
    - cp -v system.sh.gitlab system.sh
    - ./build_kernel.sh && ./tools/rebuild_deb.sh
    - du -sh deploy/linux-image-*.deb || exit 1
    - ccache -sv
    - tar --use-compress-program=lz4 -cf ccache.tar.lz4 /root/.cache/ccache/
  rules:
    - if: $CI_COMMIT_TAG
      when: never
    - when: always

build-debian-13-amd64:
  image: robertcnelson/beagle-devscripts-kernel-debian-13-amd64:latest
  # https://openbeagle.org/beagleboard/ci-docker-images
  cache:
    key: "$CI_PROJECT_NAME-debian-13-amd64"
    paths:
      - ccache.tar.lz4
  tags:
    - docker-amd64
  stage: build
  script:
    - git config --global user.email "$GITLAB_USER_EMAIL"
    - git config --global user.name "$GITLAB_USER_NAME"
    - . version.sh && echo $BRANCH
    - mkdir -p /root/.cache/ccache/ || true
    - tar --use-compress-program=lz4 -xf ccache.tar.lz4 -C / || true
    - ccache -M 1G
    - ccache -z
    - cp -v system.sh.gitlab system.sh
    - ./build_kernel.sh && ./tools/rebuild_deb.sh
    - du -sh deploy/linux-image-*.deb || exit 1
    - ccache -sv
    - tar --use-compress-program=lz4 -cf ccache.tar.lz4 /root/.cache/ccache/
  rules:
    - if: $CI_COMMIT_TAG
      when: never
    - when: always
